# Crisis feature

**`crisis`** — Tab screen (`/(tabs)/crisis`): emergency CTA, shortcuts to contacts and coping tools.

**`crisisSupport`** (sibling feature) — Stack screens under `/(config)/`: crisis log, coping tools, crisis resources.

Import crisis support views only from `@/features/crisisSupport/*`, not from this folder.
